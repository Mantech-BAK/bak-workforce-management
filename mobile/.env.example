# localhost only works when running in a web browser or an iOS simulator on the same
# Mac as the backend. For Android emulator use http://10.0.2.2:5000, and for a physical
# device (phone) use your computer's LAN IP, e.g. http://192.168.1.50:5000
EXPO_PUBLIC_API_BASE_URL=http://localhost:5000
